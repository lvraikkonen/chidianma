# 主分支归并与餐馆快速建库一期实施计划

Status: Approved by the user; implementation in progress.

Design: [餐馆快速建库一期](../specs/2026-09-04-restaurant-onboarding-design.md).

## Global constraints

Follow the current design above and AGENTS.md; this approved scope supersedes old Stage 7D.2's search-only/save-disabled and no-migration restrictions. Work in the requested existing checkout on `feat/restaurant-onboarding`; preserve unrelated untracked images. Only one implementation subagent at a time. Use focused red/green tests, task review, then whole-branch checks and review. Store collaboration artifacts in project-root qa/, not plugin/workflow folders. Never print credentials, alter existing wheel flags, seed production, or infer all database groups are the three cohort groups.

## Baseline

Re-fetch, fast-forward existing main through the wheel branch, push main and create `feat/restaurant-onboarding`. Base is `881b893`. Preserve outstanding wheel manual QA. Baseline tests, docs and diff checks must be recorded. Do not call the current runtime a full wheel rollout.

### Task 1: Minimal entry vertical slice

Read the design's Entry and knowledge section. Implement name-only Admin and Extension entry end-to-end, optional address and optional recommendation (nonblank reason only when enabled, dish optional), collapsed extra fields, and success action to existing generate/refresh behavior. Modify `packages/shared/src/entryRecovery.ts` so recommendation is optional while retaining safe uncertain-response matching and retry/recheck. Update Admin restaurant model/form and Extension quick-add controller/popup form, including address propagation. Do not add the bulk/nearby link yet (Task 4 owns it). Update the server scorer so restaurants without teammate recommendations communicate `新收录，尚无同事推荐`, preserving real other score explanations and existing scoring/order. No fabricated Recommendation rows, automatic batch refresh, unknown-value bonuses, or wheel state resets. Cover genuine recovery failures and rendered requiredness, empty optional data, no-recommendation request behavior and scoring. Run focused tests while iterating and affected package tests/typecheck before report. Do not change release version in this task. Commit only this task's files; write a detailed report with RED/GREEN evidence and concerns under qa/.restaurant-onboarding-work/task-1-report.md.

### Task 2: Backend imports, POI and persistence

Implement the design's Interfaces and persistence section, plus shared typed contracts and the pure two-column paste parser/normalizer for Task 3. Own shared onboarding contracts, API route builders, additive Prisma migration, server env/capabilities/settings and new focused onboarding service/routes. Support deterministic Mock and real Amap v3 with normalized errors, provider timeout/cancel, explicit pagination, attribution, independent signed 30-minute tickets bound to group/member/authVersion. Add a search-center settings property `{ label, latitude, longitude, coordinateSystem: "GCJ02" } | null`, validated and admin writable. POI imports accept tickets; manual imports accept name/address and explicit separate-branch confirmation. Use stable row IDs and request IDs, at most 60 rows, return per-row `created | existing | rejected` with meaningful code/message and optional restaurant ID. Persist canonical request hash/results and serialize per-group import transactions; same request replay returns receipt, divergent payload conflicts, successful receipt replay survives ticket expiry while current auth/gates remain required. Necessary provenance only, null old provenance, no overwrite/reactivation, group-scoped source uniqueness. Bulk flag and search/save flags independently enforced by routes, not merely UI. Keep existing single entry APIs compatible. Tests must cover authorization/reset/source tampering, caps and defaults, unexpected provider payloads, duplication/ambiguity, retries/concurrency/transaction rollback. Add a reproducible disposable-PostgreSQL verification script or integration test (require explicit test DB config and refuse production); execute with the controller's disposable DB. Run shared/server tests/typecheck, Prisma generation and migration checks. Report exact exported contracts/API usage for Task 3, RED/GREEN proof, commands, commit and concerns in qa/.restaurant-onboarding-work/task-2-report.md. Do not change Admin/Extension UI.

### Task 3: Admin bulk and nearby UI

Consume Task 2's shared contracts and existing Admin auth/API patterns. Build focused restaurant-onboarding components/controller with paste, editable per-row preview/selection, duplicate/ambiguous/error presentation, explicit commit and replay-safe recovery. Max 60 rows; plain names or name/address TSV. Nearby UI resolves explicit address, allows admin saving search center, supports member temporary centers, default 3000m/range 500–5000m, explicit 20-result pagination up to page 3, selected rows across one query, provider attribution and straight-line meters. Do not convert POI meters to walking minutes or invent cuisine/price/experience. Capture group/query/request identity, abort and ignore stale responses on changes; prevent submitting a stale/mixed selection. On successful import refresh restaurant list only and offer existing generate/refresh action. All errors preserve recoverable input, receipt IDs and known outcomes. Extend existing hash router to accept restaurant `mode=bulk|nearby` and groupId, independently verify Admin identity/membership, retain destination through login, never place credentials in URL. Match existing visual language and responsive/accessibility patterns. Add behavior tests for parsing integration, retry/request-ID state, group changes, pagination selection, capabilities, routing and markup; run Admin/shared tests/typecheck/build. Report under qa/.restaurant-onboarding-work/task-3-report.md with TDD and interfaces.

### Task 4: Extension links and 0.4.0 release artifacts

Add compact Admin bulk/nearby links to Extension using Task 3 hash deep-link format and current group's ID (no token). Preserve name-only QuickAdd and existing auth/session/wheel flows. Bump Extension to 0.4.0 everywhere required, including canonical release constant, manifest/package, release schema/validators and tests. Keep stable ID and exact host/permissions. Update distribution/quick-start instructions for minimal entry, imports, new source labels and same-directory upgrade; in-product remote-update notifications remain deferred. Test URL encoding, no credentials, missing membership/capabilities where relevant, package compatibility, versioned metadata and upgrade storage. Run Extension tests/typecheck/dev/internal builds and artifact validators. Report under qa/.restaurant-onboarding-work/task-4-report.md.

### Task 5: Integrated verification, review and production release

Controller owns orchestration and deployment. Run complete test/typecheck/build, Railway build, docs/artifact/secret checks and strict 0.4.0 packaging from a clean committed tree. Rehearse migration and concurrency in disposable PostgreSQL, inspect local Admin UI with real services and mock provider, then whole-branch review with all open/deferred findings. Fix through a bounded reviewed implementation task as needed. Commit current documentation and QA with truthful evidence; merge verified feature to main and push for GitHub-sourced Railway deployment (authorized user plan), observe exact revision/readiness and verifier with new gates off. Validate Amap key availability without exposing it; inspect real group records read-only and identify exactly the three existing colleague groups before enabling. Perform real provider smoke and non-destructive authenticated route checks, then enable bulk/search/save for those three IDs together, keeping wheel allowlist intact. Deliver ZIP/checksum/release JSON. Do not fabricate Chrome/colleague timing observations; request only genuinely missing operator input while completing unaffected work. Record actual deployment, flags, package checksum, checks and remaining human-only acceptance in a dated QA handoff and release record. No production seed/destructive cleanup.

## Later branches

Cross-group selected lists → daily conditions → actual meal/revisit/queue experience → freshness/facade photos → ZIP update notification. Scope each after this release; do not pre-create data structures now.
